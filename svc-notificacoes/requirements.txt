# @file svc-notificacoes/requirements.txt
# @description: dependencias do svc-notificacoes.
# Contém as libs de API, Banco de dados, consumer Kafka e envio de email via SendGrid
# e notificações push via Firebase Cloud Messaging (FCM)
#
# @author Tina de Almeida
# @date Abril 2026
# @version 1.0.0

# --- API ---
# fastapi + uvicorn → API e servidor
# pydantic → validação de dados
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic[email]==2.8.0

# --- Banco de Dados ---
# sqlalchemy + psycopg2 → ORM + driver PostgreSQL para Neon
# alembic → migrações de banco
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.0

# --- Autenticação JWT ---
# python-jose → JWT
# bcrypt → hash de senhas
# passlib → gerenciamento de senhas
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
passlib==1.7.4

# -- Mensageria --
# aiokafka → cliente Kafka assíncrono
aiokafka==0.11.0

# -- Observabilidade --
# ddtrace → integração com Datadog APM
ddtrace==2.10.0

# -- Notificações --
# sendgrid → envio de email
# firebase-admin → envio de notificações push
sendgrid==6.11.0
firebase-admin==6.5.0

# -- Testes --
# pytest → framework de testes
# pytest-asyncio → testes assíncronos
# pytest-cov → cobertura de testes
# httpx → cliente HTTP assíncrono
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.0

# -- Linting e Formatação --
# ruff → analisa o código procurando erros e bad practices (substituiu o pylint)
# black → formata o código automaticamente no padrão PEP8
# isort → organiza os imports em ordem alfabética e por tipo
ruff==0.8.4
black==25.1.0
isort==5.13.2

# -- Documentação --
# python-multipart → necessário para o FastAPI aceitar multipart/form-data
# é o formato usado para upload de arquivos (usado para importar CSV)
python-multipart==0.0.20

# @file Fim do arquivo svc-notificacoes/requirements.txt
