# @file svc-previsao/requirements.txt
# @description: dependencias do svc-previsao.
# Contém as libs de API, Banco de dados, machine learning com Prophet, MLFlow para versionamento e cache Redis.
#
# @author Tina de Almeida
# @date Abril 2026
# @version 1.0.0

# --- API ---
# fastapi + uvicorn → API e servidor
# pydantic → validação de dados
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic[email]==2.8.0

# --- Banco de Dados ---
# sqlalchemy + pymysql → ORM + driver MySQL para PlanetScale
# alembic → migrações de banco
sqlalchemy==2.0.35
pymysql==1.1.1
alembic==1.13.0

# --- Autenticação JWT ---
# python-jose → JWT
# bcrypt → hash de senhas
# passlib → gerenciamento de senhas
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
passlib==1.7.4

# -- Machine Learning --
# prophet → biblioteca de previsão de séries temporais
# mlflow → gerenciamento de experimentos de machine learning
# boto3 → cliente AWS S3 para salvar modelos
# tenacity → retentativas automáticas para operações falhas
# evidently → monitoramento de qualidade de dados e modelos
# protobuf → biblioteca de serialização de dados
prophet==1.1.5
mlflow==2.16.0
boto3==1.35.0
tenacity==9.0.0
evidently==0.4.33
protobuf==5.29.0

# -- Mensageria --
# aiokafka → cliente Kafka assíncrono
aiokafka==0.11.0

# -- Observabilidade --
# ddtrace → integração com Datadog APM
ddtrace==2.10.0

# -- Testes --
# pytest → framework de testes
# pytest-asyncio → testes assíncronos
# pytest-cov → cobertura de testes
# httpx → cliente HTTP assíncrono
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.0

# -- Linting e Formatação --
# ruff → analisa o código procurando erros e bad practices (substituiu o pylint)
# black → formata o código automaticamente no padrão PEP8
# isort → organiza os imports em ordem alfabética e por tipo
ruff==0.8.4
black==25.1.0
isort==5.13.2

# -- Documentação --
# python-multipart → necessário para o FastAPI aceitar multipart/form-data
# é o formato usado para upload de arquivos (usado para importar CSV)
python-multipart==0.0.20

# @file Fim do arquivo svc-previsao/requirements.txt
