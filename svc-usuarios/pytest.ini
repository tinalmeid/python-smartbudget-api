[pytest]
asyncio_mode = auto
testpaths = app/tests


filterwarnings =
    ignore::DeprecationWarning:sqlalchemy.*
    ignore::DeprecationWarning:jose.*

addopts =
    --cov=app
    --cov-report=term-missing
    --cov-fail-under=80
    -v
