[pytest]
asyncio_mode = auto
testpaths = app/tests


filterwarnings =
    ignore::DeprecationWarning:sqlalchemy.*

addopts =
    --cov=app
    --cov-report=term-missing
    --cov-fail-under=80
    -v
